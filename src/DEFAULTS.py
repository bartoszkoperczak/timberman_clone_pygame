RESOLUTION = 16/9

VIRTUAL_WIDTH = 1000
VIRTUAL_HEIGHT = round(VIRTUAL_WIDTH * 1 / RESOLUTION)

WINDOW_WIDTH = 1500
WINDOW_HEIGHT = round(WINDOW_WIDTH * 1 / RESOLUTION)

FPS = 60

SCALE = VIRTUAL_WIDTH / WINDOW_WIDTH

SPRITE_SIZE = (84, 100)
TREE_SIZE = (125, 100)
BRACNH_SIZE = (75, 100)
SPRITE_MOVEMENT_RANGE = TREE_SIZE[0] + SPRITE_SIZE[0]

ANIMATION_DURATION = 100 # ms

LOGS_PER_TREE = 7

PLAYER_2_STEERING = {
    'left': 1073741904,
    'right': 1073741903,
    'hit': 1073741905
}

PLAYER_1_STEERING = {
    'left': 97,
    'right': 100,
    'hit': 32
}
